import { createContext } from 'react';
export const ModalflyThemeContext = createContext({
    headerClassName: null,
    headerTextClassName: null,
    footerClassName: null,
    closeBtnClassName: null
});